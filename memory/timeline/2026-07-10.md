---
title: 2026-07-10
date: 2026-07-10
tags: [timeline]
---

## Paste-a-link job listing import → Apply & Optimize

Built the owner-requested "add a job from any link" feature (pre-cursor to a future Chrome extension). Users paste any listing URL (Indeed/LinkedIn/Greenhouse/careers page); `api/import-listing.js` fetches server-side with per-hop DNS-validated redirects (SSRF-safe), extracts via schema.org JobPosting JSON-LD → og/meta+title heuristics → ATS URL slug, upserts through the existing `upsertJobs` path (company created too), and returns the UI-shaped job. Jobs page gained a "Paste the link" entry that drops the imported listing straight into ApplyOptimizeModal — whose done-step "Apply Online →" links back to the original listing URL (the requested round-trip). Verified before writing: prod `jobs` schema + indexes via SQL — `jobs_uniq(title,company,location)` exists, so a conflicting insert returns 0 rows and the handler falls back to surfacing the aggregated duplicate; imports dedupe by `apply_url` first. Bot-walled pages (Indeed 403s) return `needs_details:true` and the modal asks the user for title/company — never guessed. Thin imports stay honestly unrated (score null). 19 new unit tests; suite 304/304; build green; handler exercised end-to-end with canned network (happy/bot-wall/manual/SSRF/re-import/conflict/405). NOT verified from sandbox: real external page fetch (egress proxy blocks job boards). New rate-limit bucket import-listing:20/hr; vercel.json maxDuration 30.

## Imports always scored at $0 — keyless ATS JSON enrichment (PR #173)

Owner decisions mid-session: (1) every pasted listing must be STORED (import = user feeding the corpus) — hardened with an upsert retry + honest stored:false; (2) every import must be SCORED with no paid API. Delivered: the canonical scorer was already free/deterministic, so the work was signal recovery — new atsApiPlan/mapAtsJson in lib/server/listingImport.js hit the keyless public JSON APIs (Greenhouse boards-api, Lever v0/postings, Ashby posting-api, SmartRecruiters v1, Workday wday/cxs) when HTML extraction comes up short (JS-rendered/bot-walled pages), and buildImportedJobRow now always scores (thin imports earn from source/title/salary/type; scoreFactors stays the legal explanation; API names beat URL-slug guesses — harness caught that precedence bug). PR #172 (feature + api/helpers.ts build fix) was MERGED by owner mid-session; branch restarted from next-migration per protocol, follow-up shipped as PR #173. Suite 307/307, build green, harness 12/12 incl. blocked-Greenhouse-recovered-via-boards-api case. GitHub MCP token expired then recovered mid-session. Sandbox egress proxy blocks job-board domains — live external fetches must be spot-checked on the Vercel preview.

## Accountability loop for imported listings (PR #175)

Owner live-tested the import feature and reported: apply-after-import "doesn't track", listing "doesn't save", and no way to report an up-but-dead listing; plus new decisions — company saved+scored on every import, and zombie listings must hurt company score. Investigation (prod runtime logs + SQL, no user-row queries): tracking and persistence actually WORKED (add_application ok twice; jobs + companies rows present) — the real gaps were reportability and visibility. Shipped PR #175: ApplyCheckpoint "Couldn't — listing was gone/closed" → report_job_availability into the existing admin reported-listings queue (keep/remove); staleListingPenalty in companyScore.js (−4 per ADMIN-CONFIRMED dead listing, cap −20, raw reports never count) threaded through fuseCompanyIntel into page + recompute paths; admin delete_listing now suppresses DB-backed listings too and recomputes the company score immediately; import-listing refuses suppressed URLs (410) and recomputes company score after every import ($0, reports corpus only, zero-evidence companies stay unscored); imports auto-save to Saved list with toast. Suite 309/309, build clean, harness 13/13.

## One-tap Apply: resume downloads in-gesture before redirect (PR #175 addition)

Owner decision: after optimization details, "Apply" should download the optimized resume PDF to the device and then redirect to the listing; email demoted to an option. Implementation constraint honored: iOS blocks downloads/popups after any await, so the PDF pre-builds in the background when the review step renders (and rebuilds after HumanProof clears pdfBlob), keeping handleApplyNow fully synchronous (anchor download → window.open(apply_url) → onApplied). pdfFailed guard prevents auto-build retry loops; Apply degrades honestly to plain "Apply online →" if the PDF can't build. Options row: Copy package · Save PDF (share-capable) · ✉ Email it (old primary flow unchanged). Commit bfb1a86 stacked onto PR #175 (title/body updated). Build 91/91, suite 309/309. Not verified live: in-tap download+redirect on a real phone.

## Admin Pro grants no longer evaporate — comped flag (PR #176)

Owner-reported: admin grants kept disappearing ("goes to 999 credits, doesn't stay"). Root cause verified on prod: admin set_pro wrote ai_credits.pro — the flag Stripe's reconcile owns; owner's account HAS a stripe_customer_id so both the on-read reconcile (any pricing/manage load) and nightly reconcile_all revoked the grant (prod row showed pro=false with stranded balance 999). Commit 671ae5a was the same bug's earlier failed fix (nonexistent users.admin_granted_pro). Shipped PR #176: migration 051 ai_credits.comped (applied to prod via MCP, owner's row restored comped=true); hasProAccess includes comped; setPro revokes filter comped=eq.false (single choke point covering all 5 revoke sites); reconcile_all excludes comped; subscription_status reads report comped as Pro; admin set_pro writes comped. Suite 312/312. Lesson: grants and lifecycle-managed flags must be separate columns — anything reconcile owns will be overwritten.

## Anti-blocking hygiene for imports (PR #177)

Owner asked to stop sites flagging import fetches as a repeated bot. Approach = look like real browser traffic + never hammer (defensible: per-user, low-volume, public pages), NOT auth/CAPTCHA evasion (explicitly excluded, along with proxy/IP rotation). Added to lib/server/listingImport.js: BROWSER_PROFILES (Chrome-Win/Chrome-mac/Safari/Firefox/iPhone, each internally consistent — UA/Sec-CH-UA/platform/Sec-Fetch all agree since a mismatch is the scraper tell; Safari/FF correctly omit Sec-CH-UA) + browserHeaders() that rotates per request-seed, adds same-host Referer. api/import-listing.js: per-host 1.5s cooldown (module map), 403/429/503 backoff-retry once with a fresh profile before falling through to the recovery ladder; Indeed m/ pages use iPhone profile. Earlier this session also merged PR #176 (Indeed embedded-JSON extraction — live-verified the real Sunrun page returns 200 with full description; the block was our extractor, not Indeed — plus challenge-page guard, hollow-row self-heal, comped admin flag). Suite 320/320, harness 17/17.

## Indeed import honest fix: paste-the-JD fallback (PR #178)

Owner: Bowlero Indeed import saved with no description. Probed the REAL prod path (seenjobs.io/api/import-listing?dry=1 against the Bowlero jk) — from Vercel serverless IP EVERY Indeed rung is blocked: direct/mobile/rpc all 403, jina gets Cloudflare Turnstile 'Just a moment'. This is IP-reputation + TLS-fingerprint blocking; header rotation can't beat it from datacenter and no free reader passes Turnstile. Earlier '200 w/ full desc' was from Supabase's cleaner IP — misleading. KEY LESSON: don't verify walled-host scraping from the Supabase http extension's IP; use the deployed prod dry-run endpoint (the http extension CAN reach seenjobs.io to do this). Fix (PR #178): never save a description-less row — require description (auto OR user-pasted) before saving; endpoint accepts {description}; needs_details returns need_description:true; ImportListingModal shows a JD textarea (80-char floor, live count) when walled; hollow rows self-heal on re-paste (auto-extract else pasted JD backfilled). Bowlero row (id 8f07bcb8, jk=7674f96082c4eae3) still hollow in prod — will heal when owner re-pastes with the JD. Suite 320/320, harness 19/19. Owner rule reaffirmed: $0, no paid scrapers.

## Smart paste for Indeed imports shipped (PR #179)

Owner screenshot showed the real problem: Indeed hard-blocks ALL server-side rungs from Vercel (verified via prod dry-run: direct/mobile/rpc 403, jina hits Cloudflare Turnstile), so the details form came back with blank title/company/location AND required a description paste — 4 fields of manual entry = "lame". Fix (PR #179, merged): a ⚡ Smart paste box at the top of the details step; user copies the whole Indeed listing once and parsePastedListing() (new lib/pastedListing.js — dependency-free, client-safe, 6 tests) splits it into title/company/location/description, pre-filling the editable fields. Handles Indeed mobile copy format, run-together "Company · Location · $Pay" headers, share URLs, star ratings, board chrome; "Full job description" marker splits header from body. Proven end-to-end: POSTed pasted-description to prod, healed owner's real Bowlero row (id 8f07bcb8) 0→259 char desc, score 63. Suite 326/326. KEY DECISION reaffirmed: Indeed is NOT free-scrapable from serverless (IP reputation + TLS fingerprint + Cloudflare); do not attempt paid proxies/CAPTCHA/auth evasion — the user-paste path is the honest reliable fallback. Session total: PRs #172-#179 all merged to next-migration (import feature, $0 scoring, accountability loop, one-tap apply, comped admin flag, Indeed embedded-JSON, anti-blocking headers, paste-JD fallback, smart paste).
