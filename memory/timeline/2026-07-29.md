---
title: 2026-07-29
date: 2026-07-29
tags: [timeline]
---

## Résumé watermark Pro-gating shipped (PR #239) + cloud env verified

Cloud session on branch `claude/verify-environment-setup-x78w1f`.

**Env verification:** Ran `scripts/cloud-setup.sh` (PR #238, merged) — Chronos brain reachable (HTTP 200, counts notes:47/facts:111/episodes:148), `npm ci` installed 70 pkgs, exit 0. seenjobs.io returns 200. Confirmed the setup script is present and wired as the web-env Setup script.

**Feature shipped — résumé watermark now Pro-gated (owner request):** The "Optimized with Seen · seenjobs.io" PDF footer previously rendered for everyone. Now it renders ONLY for free/anonymous users; Pro users get a clean, unbranded résumé.
- `buildResumePDF` (api/resume.js): draws footer only when `!meta.pro`; keeps the reserved FOOTER_BAND either way so pagination is byte-identical.
- New `resolveProAccess(req)` (lib/server/credits.js): resolves identity + effective Pro (`hasProAccess` over ai_credits: sub flag / comped / unexpired pro_until) WITHOUT charging a credit; fails safe to non-Pro.
- Both PDF paths (download_resume, email_analysis) resolve Pro and thread it through.
- Client: the two download_resume fetches (app/resume/page.tsx, components/ApplyOptimizeModal.tsx) now send `aiHeaders()` so the server can see who's Pro; email_analysis already authed.
- Tests added; 487/487 pass, `tsc --noEmit` clean. Draft PR #239 → next-migration; Vercel preview green.

**Process miss (owner caught it):** Did NOT boot the brain at session start or write back until prompted. Corrected mid-session. Reinforces CLAUDE.md rule 1 — memory boot is unconditional every session.

## UI finish pass shipped (PR #240) — one motion language, every page

Owner mandate: pure UI, animations + max simplicity for brand-new users, every page incl. employer side; product "doesn't feel finished". Session ran on Fable 5.

**Approach:** built ONE shared vocabulary instead of per-page styles — components/Reveal.tsx (the shared scroll-reveal), useIntroGate (splash→page handoff via seen:intro-done), useCountUp extended (start/delay/from-current, back-compat), globals finish layer (.rv/.sk/selection/scrollbar/focus-visible/press/route-entrance pageIn + .rise-in). All neutralized under prefers-reduced-motion.

**Landing:** staggered hero entrance timed to splash dissolve; gauge ring+number share one eased value; grade chip pops after ring settles; search focus-within glow; "Try one" chips (Amazon/Starbucks/Nike/McDonald's — verified against LIVE leaderboard, slugs = exact stored names); marketing steps got descriptions + connectors; killed the DUPLICATE footer (lp2-footer © 2025 stacked over global Footer © 2026).

**Site-wide:** .page-full upgrade animates most pages; .rise-in added to legal/terms/privacy/content-policy/reset; employer portal entrance + reveals (hub/analytics already had emp-reveal + count-ups); company page loading → skeleton (.sk), co-name capitalized. Jobs cards already staggered — untouched. Admin untouched (auth-gated).

**Real bugs found by driving the app:** (1) public/favicon.svg was 404 IN PRODUCTION (public/ had only llms.txt) — shipped brand signal-mark SVG; (2) middleware CSP blocked unsafe-eval → Next dev never hydrated → intro-guard black screen locally; fixed dev-only, prod CSP byte-identical. Also: Playwright fullPage+DPR1.5 renders tall pages black beyond viewport — capture artifact, diagnosed via live-DOM probe (all 10 reveals fire, opacity 1); use viewport shots at scroll positions instead.

**Verify:** tsc clean · 487/487 tests · prod build clean · before/after shots landing/company/jobs/employers · zero page errors. PR #240 draft → next-migration; subscribed; 1h check-in armed. Env note: pkill -f "next dev" self-matched the wrapper shell (exit 144) — use bracketed patterns.

## Mobile-first correction on PR #240 after owner iPhone review

Owner reviewed the preview on a real iPhone and correctly called the hero not-mobile-designed (orphaned McDonald's chip, trust line breaking mid-phrase across two fonts, proof card buried two screens down). Root cause: desktop decisions inherited via flex-wrap; my 390px check had only covered above-the-fold.

Fix (commit 8de421e, pushed to PR #240, preview redeployed Ready): mobile reorder via display:contents (headline → search → PROOF CARD → chips → trust — thesis on first scroll); 2×2 chip grid under 560px with label on its own line; trust row becomes a ✓ checklist (one font, no mid-phrase wraps); restored pinch-zoom (removed viewport maximumScale:1 — WCAG 1.4.4) and killed the iOS focus-zoom it reintroduces by forcing all inputs to 16px on phones (globals, !important beats inline-styled small inputs); lede .98rem under 520px. Verified at 390px: search + full proof card + chips + checklist in ONE viewport, zero page errors; tsc clean; prod build clean.

IMPORTANT dev-env finding: /jobs + /companies throw "Cannot find module './5873.js'" in NEXT DEV MODE ONLY (reproducible across rm -rf .next; chunk ids content-deterministic). Production build + next start serves ALL routes 200 (verified locally; CI verify + Vercel preview green). Pre-existing — those pages untouched by this PR (empty diff vs base). Suspect Next 15.5.19 webpack-dev chunking bug; consider Next patch bump later. Also: the sandbox proxy 403s *.vercel.app (can't curl previews from cloud sessions; seenjobs.io is allowed).

## Aurora Glass applied (commit 6af7349, PR #240) after template roasting → owner picked D

Owner rejected all five direction mockups as recognizable AI-template looks (permanent design_bar rule recorded), then picked Aurora Glass (D) with the condition "layout must be perfect". Implemented as ONE token system: --glass/--glass-strong/--glass-line/--glass-hi in globals + @supports solid fallback; richer aurora radials; shared surfaces (lp2 cards/steps/final CTA, nav dropdowns) consume tokens; hero search/chips/eyebrow frosted, proof card gets masked gradient-ring border (indigo→green) on deep glass; rhythm normalized. Layout audit AUTOMATED: 320/390/768/1024/1440 → 0px horizontal overflow everywhere, no orphan chips (2×2 mobile, 1 row ≥768). tsc clean, tests pass, prod build clean, pushed to PR #240. Read-receipt "Seen ✓✓" concept remains on the shelf as future brand language. Next: owner reviews preview; extend glass tokens deeper (company page, jobs cards) in follow-up if owner approves the landing treatment.

## PR #240 MERGED to next-migration (auto-deploys to prod)

Owner said "Merge it" — marked ready + merged (merge commit 20de9c1, all 5 checks green). Ships to seenjobs.io: (1) UI finish pass — Reveal/useIntroGate/useCountUp motion system, splash→hero handoff, gauge+number single eased value, Try-one chips (amazon/starbucks/nike/mcdonalds), skeletons, one footer, favicon.svg (was 404 in prod!), dev-only CSP unsafe-eval fix, pinch-zoom restored + 16px mobile inputs; (2) mobile-first hero — proof card on first scroll, 2×2 chips, ✓ checklist; (3) Aurora Glass material system (owner-picked direction D) — glass tokens, gradient-ring proof card, richer aurora, layout audit 0px overflow at 320/390/768/1024/1440; (4) brain serializer fix (Opus agent, commit 598cf4a) — write/parse now exact inverses, 545/545 tests. REMAINING for owner: run `node scripts/brain-repair.mjs --apply` on the Mac to repair the single corrupted fact (watermark_policy, 4094 backslashes; dry-run verified, 0 ambiguous). Local branch reset onto merged base per merged-PR rule. Next UI step if owner wants: extend glass tokens through company page / jobs cards / employer surfaces.
