---
title: 2026-07-05
date: 2026-07-05
type: daily
tags: [timeline, revenue, growth]
sessions: 1
---

# 2026-07-05 → 06 — Operation 50% build

*Reconstructed from `CLAUDE_HANDOFF.md` (2026-07-06 "Operation 50% build"
section) when the vault was seeded. See [[operation-50]] for the plan.*

## Shipped (PRs, all squash-merged to `next-migration` unless noted)
- **#157 Weekly Ghost Report** — `/ghost-report` page + `opengraph-image` share
  card + admin `GhostReportPanel` (copy-caption tool) + pure
  `lib/server/ghostReport.js` (8 tests). No owner setup; reads existing data.
  See [[behavioral-flywheel]].
- **#158 Outcome email loop** — Day-7/14/30 follow-ups.
  `api/outcome-followups.js` (daily cron 15:00 UTC, routes the Vercel cron GET),
  `lib/server/outcomeEmails.js` (pure, 7 tests, HMAC unsubscribe),
  `api/unsubscribe.js`. Runs on the already-live `RESEND_KEY` + verified
  `noreply@seenjobs.io` — works on deploy. `email_prefs` opt-out.
- **#159 report snapshot** + **#160 report view/keep/delete** — reported
  ephemeral (`j_<hash>`) listings viewable/actionable in admin. Snapshot columns
  capture title/company/city/apply_url at report time; admin can open, Keep
  (dismiss), or Delete (real suppression via `suppressed_listings`, filtered in
  `api/jobs.js` search).
- **#161** — search-card report snapshot fix + Employer revenue engine E4
  (checkout) + Seen Live poll-based realtime v1 (global toast system).
- **#162** — Seen Live INSTANT (Supabase Realtime broadcast → <1s push) +
  complete [[employer-engine|Employer engine]] (fulfillment, perks, badges).
- **#163 Employer portal** — **OPEN, preview green, NOT merged at handoff.**
  Makes `/employers` employer-first (seeker Nav/Footer hidden on `/employers*`),
  reputation dashboard, promote section; `account_type` separation label.

## Migrations applied to prod (verified by SQL): 045–050
`045_outcome_email_log` · `046_job_report_snapshot` · `047_suppressed_listings` ·
`048_employer_purchases` · `049_employer_perks` · `050_account_type`.
(`044_pro_until` applied the prior session.) See [[database]].

## Key architecture established
- [[seen-live]] realtime: admin uses custom-token + service-key reads (no
  Supabase session / no RLS) → postgres_changes NOT viable; instead a
  non-sensitive broadcast ping on channel `seen-live` triggers an authenticated
  cursor fetch; 12s poll is the fallback.
- [[employer-engine]] E4: SKUs `featured30` $79/30d, `verified90` $249/90d;
  email-based no-login Stripe checkout; idempotent fulfillment; admin grants
  time-boxed perks. **Integrity: money NEVER changes a transparency score.**
- Employer/seeker separation: `/employers` hides seeker chrome; portal writes no
  seeker records; `profiles.account_type` is the DB label.

## Decisions
- Candidate sourcing is a deliberate **Phase 2** (owner: "portal on real data
  first") — needs a candidate-profile opt-in model before employers browse
  talent. Not built/faked. Logged in [[decisions/log]].

## Next
- Merge/confirm #163. Phase-2 candidate profiles + employer sourcing
  (owner-gated). Company-page GEO restructure still pending. → carried into
  [[timeline/2026-07-06]].
