# Deploy notes

Session G fixes deployed:
- admin KPI grid: repeat(4,1fr) → auto-fit minmax(140px,1fr) (no breakpoint needed)
- admin responsive breakpoint: 700px → 900px (catches zoomed-out Safari)
- adm-wrap: overflow-x:hidden in base style
- old index.html / employer.html moved to _reference/
