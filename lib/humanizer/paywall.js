// HumanProof paywall copy — single source of truth for the locked-CTA wording. NO AI.
//
// FRAMING (locked to spec): the copy ONLY talks about removing generic AI tone and
// adding truthful work context so the application reads real and is interview-ready. It
// must NEVER mention bypassing, beating, tricking, or evading detectors / ATS /
// employers. Keeping the strings here means the API and the UI can't drift.
export function humanProofLockCopy() {
    return {
        headline: 'Your application is optimized. Now make it sound real.',
        body: 'Unlock HumanProof to remove generic AI tone, add truthful work context, and make your application interview-ready.',
        cta: 'Unlock HumanProof',
    };
}
